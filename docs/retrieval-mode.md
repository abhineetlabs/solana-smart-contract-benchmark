# Retrieval Mode

Retrieval mode is planned but not active in the first milestone.

When enabled, it will use a pinned local corpus rather than live internet content so runs remain comparable over time.
