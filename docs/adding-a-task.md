# Adding a Task

Create a new task under `tasks/<task_id>/` with:

- `core/spec.json`
- `core/prompt.md`
- one or more track folders
- `track.config.json`
- starter scaffold
- public tests
- reference solution

Use `./benchmark validate` after adding or editing a task.
