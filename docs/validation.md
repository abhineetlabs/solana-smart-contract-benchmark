# Validation

The benchmark should only be trusted after the harness itself is validated.

Current validation focus:

- task discovery works
- task specs are structurally valid
- editable files exist in starter scaffolds
- track configs point at valid workspace content

Next validation phase:

- reference solutions pass all checks
- insecure solutions fail expected security checks
- hidden test leakage is prevented
